// Напиши функцію findLongestWord(string), яка приймає параметром довільний рядок
//  (в рядку будуть тільки слова і прогалини)
//  і повертає найдовше слово в цьому рядку.

const maxWord = prompt('Введіть слова')
function findLongestWord(string){
    maxWord.split(' ')
    for (let i = 0; i < maxWord; i++){
        maxWord.length > maxWord[i]
    }
}
console.log(maxWord)